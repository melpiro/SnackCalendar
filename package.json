{
  "name": "snack-calender",
  "version": "1.0.0",
  "description": "Snack panner for a classroom",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "rm -rf dist && parcel serve './src/*.html' -p 8080",
    "build": "rm -rf public && parcel build ./src/*.html --public-url / --out-dir public"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.0.0-rc.0",
    "@parcel/validator-typescript": "^2.8.3",
    "@types/node": "^18.15.3",
    "assert": "^2.0.0",
    "buffer": "^5.7.1",
    "parcel": "^1.12.3",
    "posthtml-include": "^1.7.4",
    "stream-browserify": "^3.0.0",
    "typescript": "^4.9.5",
    "util": "^0.12.5"
  },
  "dependencies": {
    "animejs": "^4.1.3",
    "firebase": "^10.14.1",
    "parcel-bundler": "^1.12.5"
  }
}
